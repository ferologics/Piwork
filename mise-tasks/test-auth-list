#!/usr/bin/env bash
#MISE description="List auth store entries (default profile, test harness)"
set -euo pipefail

if ! nc -z localhost 19385 2>/dev/null; then
    echo "Test server not running. Run: mise run test-start"
    exit 1
fi

RESULT=$(python <<'PY' | nc -w 2 localhost 19385
import json

payload = {
    "cmd": "auth_list",
}
print(json.dumps(payload))
PY
)

if [[ "$RESULT" == ERR:* ]]; then
    echo "$RESULT"
    exit 1
fi

echo "$RESULT"
