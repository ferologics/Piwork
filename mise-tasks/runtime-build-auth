#!/usr/bin/env bash
#MISE description="Force rebuild runtime with auth baked in"
set -euo pipefail

ROOT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)

PIWORK_COPY_AUTH=1 mise run -f runtime-build
