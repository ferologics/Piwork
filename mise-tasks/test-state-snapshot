#!/usr/bin/env bash
#MISE description="Fetch structured UI/runtime state snapshot (test harness)"
set -euo pipefail

if ! nc -z localhost 19385 2>/dev/null; then
    echo "Test server not running. Run: mise run test-start"
    exit 1
fi

echo '{"cmd":"state_snapshot"}' | nc -w 3 localhost 19385
