#!/usr/bin/env bash
#MISE description="Dump current UI state (test harness)"
set -euo pipefail

if ! nc -z localhost 19385 2>/dev/null; then
    echo "Test server not running. Run: mise run test-start"
    exit 1
fi

echo "{\"cmd\":\"dump_state\"}" | nc -w 2 localhost 19385
