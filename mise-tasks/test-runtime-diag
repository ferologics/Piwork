#!/usr/bin/env bash
#MISE description="Fetch runtime taskd diagnostics JSON (test harness)"
set -euo pipefail

if ! nc -z localhost 19385 2>/dev/null; then
    echo "Test server not running. Run: mise run test-start"
    exit 1
fi

echo '{"cmd":"runtime_diag"}' | nc -w 3 localhost 19385
