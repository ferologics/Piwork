# TODO

- Add **account/login flow** for provider subscriptions (pi `/login` OAuth + API keys).
- Persist credentials per account and allow switching profiles.
- Surface auth state in UI (profile chip + active provider/model).
